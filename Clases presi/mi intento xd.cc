#include <iostream>
#include <ctime>
#include <cstdlib>
std::string GenerarcUPON(std::string prefijo);

int main()
{
    std::string prefijo, boleto;
    std::cout << "Por favor ingrese el texto que desea agregar al boleto" << std::endl;
    std::cin >> prefijo;
    boleto = 

    return 0;
}
std::string GenerarcUPON(std::string prefijo)
{
    // Generar numeros random
    int randomnum = rand() % 101;
    // concatenar el texto
    return;
}